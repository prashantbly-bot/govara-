window.GoVaraApp=(function(){const nav=[['dashboard','dashboard'],['system','system'],['roles','roles'],['policies','policies'],['operations','operations'],['financial','financial'],['documents','documents'],['audit','audit'],['api','api']];function dashboard(){const c=GoVaraState.systemConfig||{};return `<div class="page-head"><h1>GoVara Administrator Control Center</h1><div class="muted">Modular frontend shell — all control tabs are registered.</div></div><div class="grid four"><div class="card"><div class="metric">READY</div><div class="metric-label">Frontend</div></div><div class="card"><div class="metric">${GoVaraAPI.getAPIUrl()?'CONFIGURED':'NOT CONFIGURED'}</div><div class="metric-label">Consolidated API</div></div><div class="card"><div class="metric">${c.environment||'TESTING'}</div><div class="metric-label">Environment</div></div><div class="card"><div class="metric">BLOCKED</div><div class="metric-label">Real Money</div></div></div><div class="grid two"><section class="card"><h2>Architecture</h2><div class="notice safe">index.html is only the shell. 26A–27 are independent modules.</div><p>26A System → 26B Roles → 26C Policies → 26D Operations → 26E Financial → 26F Documents → 26G Audit → STEP 27 API.</p></section><section class="card"><h2>Safety</h2><p>Real Money: <b>BLOCKED</b></p><p>Real Payment: <b>BLOCKED</b></p><p>Bank Transfer: <b>BLOCKED</b></p><p>Frontend Financial Authority: <b>NO</b></p></section></div>`}function render(){const p=GoVaraState.currentPage;let body;if(p==='dashboard')body=dashboard();else{const name=GoVaraRouter.pages[p];body=window[name].render()}document.getElementById('app').innerHTML=`<div class="app-shell"><aside class="sidebar"><div class="brand">GoVara</div><div class="brand-sub">Administrator Control Center</div><div class="nav">${nav.map(([id,k])=>`<button class="${p===id?'active':''}" data-nav="${id}">${GoVaraI18N.t(k)}</button>`).join('')}</div></aside><main class="main"><header class="topbar"><div class="statuses"><span class="badge">FRONTEND READY</span><span class="badge">${GoVaraAPI.getAPIUrl()?'API CONFIGURED':'API NOT CONFIGURED'}</span><span class="badge">TESTING MODE</span><span class="badge blocked">REAL MONEY BLOCKED</span></div><div class="top-actions"><select id="globalLanguage">${GoVaraI18N.options().filter(([c])=>GoVaraState.systemConfig?.availableLanguages?.[c]??(['en','hi'].includes(c))).map(([c,n])=>`<option value="${c}" ${GoVaraState.uiLanguage===c?'selected':''}>${n}</option>`).join('')}</select></div></header><section class="content">${body}</section></main></div>`;document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>GoVaraRouter.go(b.dataset.nav));document.getElementById('globalLanguage').onchange=e=>GoVaraI18N.apply(e.target.value);const name=GoVaraRouter.pages[p];if(name&&window[name]?.bind)window[name].bind()}return{render}})();document.addEventListener('DOMContentLoaded',()=>GoVaraApp.render());
